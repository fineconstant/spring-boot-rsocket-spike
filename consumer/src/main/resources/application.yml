spring:
  application:
    name: spring-boot-rsocket-consumer
server:
  port: 8081
management:
  endpoints:
    web:
      exposure:
        include: '*'
rsocket:
  producer:
    host: localhost
    port: 7000
